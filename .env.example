# Database
DATABASE_URL=postgresql://<user>:<password>@<neon-host>/<db>?sslmode=require
DATABASE_URL_UNPOOLED=postgresql://<user>:<password>@<neon-host-unpooled>/<db>?sslmode=require

# AI
OPENROUTER_API_KEY=<openrouter-key>

# Google OAuth
GOOGLE_CLIENT_ID=<google-client-id>
GOOGLE_CLIENT_SECRET=<google-client-secret>
NEXT_PUBLIC_GOOGLE_PLACES_API_KEY=<google-places-browser-key>

# Spotify OAuth
SPOTIFY_CLIENT_ID=<spotify-client-id>
SPOTIFY_CLIENT_SECRET=<spotify-client-secret>

# Email
RESEND_API_KEY=<resend-key>
RESEND_FROM_EMAIL=ITK <hello@itk.so>
RESEND_REPLY_TO_EMAIL=reply@itk.so
RESEND_INBOUND_WEBHOOK_SECRET=<long-random-secret>
# Note: Resend inbound parsing requires a verified custom domain inbox.
# onboarding@resend.dev works for outbound testing but not inbound routing.

# App URLs
NEXT_PUBLIC_APP_URL=https://itk-so.vercel.app
BACKEND_API_URL=https://backend-fawn-xi-15.vercel.app

# Security
SESSION_SECRET=<long-random-secret>
TOKEN_ENCRYPTION_KEY=<long-random-secret>
API_CRON_SECRET=<long-random-secret>

# CORS
CORS_ORIGINS=http://localhost:3000,https://itk-so.vercel.app
